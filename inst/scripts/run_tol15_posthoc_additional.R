#!/usr/bin/env Rscript

# Compatibility stub retained so historical command paths fail explicitly.

stop(
  paste(
    "PPMWASS_LEGACY_DRIVER_DISABLED: run_tol15_posthoc_additional.R reads",
    "superseded distance/output layouts and applies pre-audit query bootstrap",
    "and tie rules. Use run_main_retrieval_publication.R and",
    "run_end_to_end_reranking_publication.R."
  ),
  call. = FALSE
)
