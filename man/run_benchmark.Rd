% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{run_benchmark}
\alias{run_benchmark}
\title{Run Benchmark Suite}
\usage{
run_benchmark(
  spectra_result,
  methods = c("entropy", "hellinger", "cosine"),
  ground_truth = NULL,
  params = eihrms_default_params(),
  cluster_method = c("kmeans", "dbscan"),
  kmeans_nstart = 10,
  dbscan_eps = 0.5,
  dbscan_minPts = 3,
  umap_neighbors = NULL,
  apply_weights = FALSE,
  weight_intensity_power = 0.5,
  weight_freq_power = 0.5,
  weight_bin_width = 1,
  apply_loss_weights = TRUE,
  threshold_similarity_transform = c("minmax", "linear_clamp", "reciprocal",
    "exponential", "linear"),
  threshold_similarity_scale = NULL
)
}
\arguments{
\item{spectra_result}{Output from build_spectra() or build_spectra_from_msp().}

\item{methods}{Character vector of distance methods to compare.}

\item{ground_truth}{Optional data frame with true groupings (e.g., homolog series).}

\item{params}{Parameter list.}

\item{cluster_method}{Clustering method ("kmeans" or "dbscan") for evaluation.}

\item{kmeans_nstart}{Number of random starts for k-means.}

\item{dbscan_eps}{Epsilon for DBSCAN (only used if cluster_method = "dbscan").}

\item{dbscan_minPts}{Minimum points for DBSCAN (only used if cluster_method = "dbscan").}

\item{umap_neighbors}{Number of UMAP neighbors (NULL uses min(15, n - 1)).}

\item{apply_weights}{If TRUE, apply spectral weighting before distance calculation.}

\item{weight_intensity_power}{Intensity power for preprocessing weights.}

\item{weight_freq_power}{m/z frequency power for preprocessing weights.}

\item{weight_bin_width}{Bin width for m/z frequency calculation.}

\item{apply_loss_weights}{If TRUE, apply intensity weighting to loss spectra.}

\item{threshold_similarity_transform}{Distance-to-similarity transform used
for threshold-based metrics (FDR and homolog detection).}

\item{threshold_similarity_scale}{Optional scale for the exponential transform.}
}
\value{
A list containing benchmark results for each method.
}
\description{
Comprehensive benchmark comparing multiple distance methods.
}
