% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lc_workflow.R
\name{lc_search_plan}
\alias{lc_search_plan}
\title{Define an LC Library-search Plan}
\usage{
lc_search_plan(
  precursor_ppm = 10,
  rt_window = NULL,
  require_ion_mode = TRUE,
  require_adduct = FALSE,
  collision_energy_tolerance = NULL,
  missing_metadata = c("allow", "exclude", "error"),
  exclude_same_id = FALSE,
  first_pass_method = "entropy_weighted",
  rerank_method = "ppm_wasserstein",
  first_pass_top_k = 50L,
  final_top_k = 10L
)
}
\arguments{
\item{precursor_ppm}{Maximum precursor-mass difference in ppm, or \code{NULL}.}

\item{rt_window}{Maximum absolute retention-time difference, in the same
unit used by query and library metadata, or \code{NULL}.}

\item{require_ion_mode}{Require matching positive/negative ion mode.}

\item{require_adduct}{Require matching normalized adduct labels.}

\item{collision_energy_tolerance}{Maximum absolute collision-energy
difference, or \code{NULL}.}

\item{missing_metadata}{Policy for values missing from an active gate:
\code{"allow"}, \code{"exclude"}, or \code{"error"}.}

\item{exclude_same_id}{Remove library entries whose ID equals the query ID.}

\item{first_pass_method, rerank_method}{Distance methods passed to
\code{\link[=search_topk_rerank]{search_topk_rerank()}}.}

\item{first_pass_top_k, final_top_k}{Candidate and returned hit counts.}
}
\value{
An object of class \code{LCSearchPlan}.
}
\description{
The plan separates metadata-based candidate gates from spectral scoring so
that every reduction in the candidate set can be audited. A \code{NULL} numeric
gate disables that gate.
}
