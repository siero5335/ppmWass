% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_sweep.R
\name{sweep_distance_params}
\alias{sweep_distance_params}
\title{Sweep Distance Parameters Across a Benchmark Grid}
\usage{
sweep_distance_params(
  spectra_result,
  grid,
  methods = c("entropy", "hellinger", "cosine"),
  ground_truth = NULL,
  params = eihrms_default_params(),
  metrics = NULL,
  n_replicates = 1,
  sample_frac = 1,
  seed = 42,
  keep_results = FALSE,
  progress = TRUE,
  ...
)
}
\arguments{
\item{spectra_result}{Output from \code{build_spectra()} or \code{build_spectra_from_msp()}.}

\item{grid}{Parameter combinations as either a data frame or a named list
that will be expanded with \code{expand.grid()}.}

\item{methods}{Character vector of distance methods to compare.}

\item{ground_truth}{Optional data frame with true groupings.}

\item{params}{Base parameter list.}

\item{metrics}{Optional subset of summary metric column names to keep.
\code{NULL} keeps all metrics returned by \code{run_benchmark()}.}

\item{n_replicates}{Number of repeated benchmark runs per grid row.}

\item{sample_frac}{Fraction of spectra to sample without replacement for each
replicate. Use values below 1 to estimate stability by repeated
subsampling; \code{1} reuses the full dataset.}

\item{seed}{Random seed used for replicate subsampling.}

\item{keep_results}{If \code{TRUE}, also return the full benchmark object for each
grid row.}

\item{progress}{If \code{TRUE}, print sweep progress.}

\item{...}{Additional arguments passed to \code{run_benchmark()}.}
}
\value{
A list with \code{summary}, \code{grid}, and optionally \code{results}.
}
\description{
Runs \code{run_benchmark()} repeatedly over a grid of distance-parameter settings
and collects the summary metrics into one tidy table.
}
\details{
By default each grid row is evaluated once on the full dataset. Set
\code{n_replicates > 1} together with \code{sample_frac < 1} to obtain repeated
subsampled estimates that can be summarized into confidence intervals or
stability plots for manuscript reporting.
}
