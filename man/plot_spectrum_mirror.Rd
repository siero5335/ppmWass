% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spectra_compare.R
\name{plot_spectrum_mirror}
\alias{plot_spectrum_mirror}
\title{Spectrum comparison plots (mirror / overlay)}
\usage{
plot_spectrum_mirror(
  specA,
  specB,
  ppm = 20,
  use_aligned_bins = TRUE,
  normalize = c("max", "sum", "none"),
  scale = 100,
  sample_names = c("A", "B"),
  alpha_shared = 1,
  alpha_unique = 0.4,
  label_top_n = 10,
  label_min = NULL,
  label_digits = 4,
  title = NULL,
  subtitle = NULL,
  xlim = NULL,
  file = NULL,
  width = 9,
  height = 5
)
}
\arguments{
\item{specA, specB}{Two spectra matrices (columns \code{mz}, \code{intensity}).}

\item{ppm}{PPM tolerance used for binning/alignment and match shading.}

\item{use_aligned_bins}{If TRUE, plot aligned/binned spectra using \code{align_spectra()}.}

\item{normalize}{Intensity scaling for display: \code{max} (default), \code{sum}, or \code{none}.}

\item{scale}{Display scale when \code{normalize != 'none'} (e.g., 100).}

\item{sample_names}{Length-2 character vector for legend labels.}

\item{alpha_shared}{Alpha for peaks/bins present in both spectra (raw mode).}

\item{alpha_unique}{Alpha for peaks present only in one spectrum (raw mode).}

\item{label_top_n}{Number of peaks to label per spectrum (0 to disable).}

\item{label_min}{Minimum absolute displayed intensity required for labeling.}

\item{label_digits}{Number of digits for m/z labels.}

\item{title, subtitle}{Plot title/subtitle.}

\item{xlim}{Optional x-axis limits (numeric length 2).}

\item{file}{Optional output file path (e.g., .pdf, .png) via \code{ggplot2::ggsave}.}

\item{width, height}{Output size for \code{file}.}
}
\value{
A ggplot object.
}
\description{
ggplot2-based utilities to visually compare two spectra. These functions are designed
to work directly with the internal spectrum matrices used by the package
(two-column matrices with columns \code{mz} and \code{intensity}).
}
\details{
The mirror plot supports two modes:
\itemize{
\item \code{use_aligned_bins = TRUE} (default): uses \code{align_spectra()} to bin/align peaks
using a ppm-dependent window (consistent with distance calculations).
\item \code{use_aligned_bins = FALSE}: plots raw peaks as-is; matched peaks are optionally
identified by ppm windows for alpha shading.
}
}
