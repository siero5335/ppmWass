% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spectra_compare.R
\name{plot_pair_contribution_bar}
\alias{plot_pair_contribution_bar}
\title{Plot contribution breakdown for a pair}
\usage{
plot_pair_contribution_bar(
  det,
  title = "Distance contribution (squared)",
  subtitle = NULL,
  label_min = 0.08,
  style = c("auto", "two_level", "simple"),
  loss_breakdown_min = 0.05,
  show_legend = TRUE,
  base_size = 10
)
}
\arguments{
\item{det}{A list returned by \code{distance_breakdown_pair()}.}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle.}

\item{label_min}{Minimum fraction (0-1) required to label a segment.}

\item{style}{Plot style: \code{auto} (default), \code{two_level}, or \code{simple}.}

\item{loss_breakdown_min}{Minimum \code{loss} fraction (of total) required to show the loss-composition bar.}

\item{show_legend}{If TRUE, show a legend.}

\item{base_size}{Base font size passed to \code{ggplot2::theme_minimal()}.}
}
\value{
A ggplot object.
}
\description{
Provides a compact, report-friendly visualization of which components drive the
(squared) distance for a given pair, using the contribution fields returned by
\code{distance_breakdown_pair()}.
}
\details{
By default (\code{style='auto'}), this function draws a \strong{two-level} view when possible:
a total bar (\code{frag} vs \code{loss}) plus a second bar showing the \emph{composition of loss}
(e.g., anchored vs pairwise, and/or raw vs typical) \strong{normalized within loss}.
This tends to remain readable even when \code{loss} is a small fraction of the total.
}
