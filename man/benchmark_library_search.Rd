% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_library.R
\name{benchmark_library_search}
\alias{benchmark_library_search}
\title{Benchmark Query-vs-Library Search Across Methods}
\usage{
benchmark_library_search(
  query_spectra,
  library_spectra,
  methods = c("cosine", "entropy", "wasserstein"),
  params = eihrms_default_params(),
  top_k = c(1, 5, 10, 20),
  inchikey_chars = 14,
  ri_tolerance = NULL,
  keep_search_results = TRUE,
  search_top_n = 10,
  progress = TRUE
)
}
\arguments{
\item{query_spectra}{Output from \code{build_spectra()} or \code{build_spectra_from_msp()}.}

\item{library_spectra}{Output from \code{build_spectra()} or \code{build_spectra_from_msp()}.}

\item{methods}{Character vector of distance methods to compare.}

\item{params}{Base parameter list.}

\item{top_k}{Integer vector of cutoffs for Precision@K.}

\item{inchikey_chars}{Number of InChIKey prefix characters used for matching.}

\item{ri_tolerance}{Optional RI tolerance passed to candidate filtering after distance calculation.}

\item{keep_search_results}{If \code{TRUE}, also return top-ranked hit tables from \code{search_library()}.}

\item{search_top_n}{Number of hits to export per query when \code{keep_search_results = TRUE}.}

\item{progress}{If \code{TRUE}, print progress messages.}
}
\value{
A list with per-method results and a \code{summary} data frame.
}
\description{
Runs \code{compute_distance_matrix_search()} for one or more methods and evaluates
retrieval quality against library entries sharing the same InChIKey prefix.
Nonfinite distances and RI-excluded entries are not ranked. Queries with a
relevant entry in the original library remain in the evaluation denominator;
if no candidate survives, their metrics are zero. Average precision uses the
original number of relevant library entries, and Precision@K divides by the
smaller of K and the number of surviving candidates. With an RI tolerance,
pairs with missing RI values are excluded, as in \code{search_library()}.
}
