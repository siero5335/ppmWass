% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance_compat.R
\name{compute_distance_matrix_search}
\alias{compute_distance_matrix_search}
\title{Compute Query-vs-Library Distance Matrix}
\usage{
compute_distance_matrix_search(
  query_frag_list,
  query_loss_list,
  lib_frag_list,
  lib_loss_list,
  params,
  progress = TRUE,
  query_loss_typ_list = NULL,
  lib_loss_typ_list = NULL,
  query_mref_conf = NULL,
  lib_mref_conf = NULL,
  query_loss_anchor_list = NULL,
  query_loss_pair_list = NULL,
  lib_loss_anchor_list = NULL,
  lib_loss_pair_list = NULL,
  query_loss_anchor_typ_list = NULL,
  query_loss_pair_typ_list = NULL,
  lib_loss_anchor_typ_list = NULL,
  lib_loss_pair_typ_list = NULL
)
}
\arguments{
\item{query_frag_list}{Named list of query fragment spectra.}

\item{query_loss_list}{Named list of query loss spectra.}

\item{lib_frag_list}{Named list of library fragment spectra.}

\item{lib_loss_list}{Named list of library loss spectra.}

\item{params}{Parameter list from \code{\link[=eihrms_default_params]{eihrms_default_params()}}.}

\item{progress}{If \code{TRUE}, emit progress messages.}

\item{query_loss_typ_list, lib_loss_typ_list}{Optional typical-loss spectra.}

\item{query_mref_conf, lib_mref_conf}{Optional Mref confidence vectors.}

\item{query_loss_anchor_list, query_loss_pair_list}{Optional query split-loss spectra.}

\item{lib_loss_anchor_list, lib_loss_pair_list}{Optional library split-loss spectra.}

\item{query_loss_anchor_typ_list, query_loss_pair_typ_list}{Optional query typical split-loss spectra.}

\item{lib_loss_anchor_typ_list, lib_loss_pair_typ_list}{Optional library typical split-loss spectra.}
}
\value{
A numeric matrix of dimension \verb{nquery x nlibrary}.
}
\description{
Computes a rectangular query-by-library distance matrix. By default this uses
the legacy exact pair-loop backend. Set \code{params$backend = "sparse_bins"} to
opt into the experimental sparse-bin backend for batchable methods.
}
\details{
The sparse-bin backend is approximate and currently supports only \code{cosine},
\code{weighted_cosine}, and \code{hellinger} without typical-loss, split-loss, or
Mref-confidence options. Unsupported sparse-bin configurations fall back to
the legacy backend with a message instead of silently ignoring inputs.
}
