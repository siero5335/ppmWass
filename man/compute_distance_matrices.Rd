% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance_matrix.R
\name{compute_distance_matrices}
\alias{compute_distance_matrices}
\title{Compute Distance Matrices with Component Breakdown}
\usage{
compute_distance_matrices(
  frag_list,
  loss_list,
  params,
  progress = TRUE,
  loss_typ_list = NULL,
  mref_conf = NULL,
  loss_anchor_list = NULL,
  loss_pair_list = NULL,
  loss_anchor_typ_list = NULL,
  loss_pair_typ_list = NULL
)
}
\arguments{
\item{frag_list}{Named list of fragment spectra.}

\item{loss_list}{Named list of loss spectra (combined/legacy).}

\item{params}{Parameter list.}

\item{progress}{If TRUE, print progress messages.}

\item{loss_typ_list}{Optional list of typical-loss spectra for combined loss.}

\item{mref_conf}{Optional named numeric vector of Mref confidence (0..1).}

\item{loss_anchor_list}{Optional list of anchored-loss spectra (A).}

\item{loss_pair_list}{Optional list of pairwise-loss spectra (B).}

\item{loss_anchor_typ_list}{Optional list of typical-loss spectra for anchored losses.}

\item{loss_pair_typ_list}{Optional list of typical-loss spectra for pairwise losses.}
}
\value{
A list with:
\itemize{
\item dist: distance matrix (same as compute_distance_matrix())
\item components: named list of matrices (per-component distances/weights)
}
}
\description{
This function computes the main distance matrix \strong{and} (optionally) the distances/weights
for each sub-component (fragment vs loss, and for split-loss mode: anchored vs pairwise).
It is intended for diagnostics, interpretation, and plotting.
}
