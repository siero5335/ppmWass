% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_topk_rerank.R
\name{search_topk_rerank}
\alias{search_topk_rerank}
\title{Search a Library with Top-K Candidate Reranking}
\usage{
search_topk_rerank(
  query_frag_list,
  query_loss_list = NULL,
  lib_frag_list,
  lib_loss_list = NULL,
  params,
  first_pass_method = "entropy_weighted",
  rerank_method = "ppm_wasserstein",
  first_pass_top_k = 50L,
  final_top_k = 10L,
  query_ids = names(query_frag_list),
  lib_ids = names(lib_frag_list),
  exclude_self = NULL,
  first_pass_block_size = NULL,
  return_first_pass = TRUE,
  use_parallel = params$use_parallel \%||\% FALSE,
  n_cores = params$n_cores \%||\% 1L,
  progress = TRUE,
  query_loss_typ_list = NULL,
  lib_loss_typ_list = NULL,
  query_mref_conf = NULL,
  lib_mref_conf = NULL,
  ...
)
}
\arguments{
\item{query_frag_list, lib_frag_list}{Named lists of two-column fragment
spectra matrices.}

\item{query_loss_list, lib_loss_list}{Optional named lists of two-column
neutral-loss spectra matrices. Both must be \code{NULL} for fragment-only
search, or both must be non-\code{NULL} for fragment-plus-loss search.}

\item{params}{Parameter list. \code{params$distance_method} is overridden for the
first-pass and rerank stages.}

\item{first_pass_method}{Distance method used to generate candidates.}

\item{rerank_method}{Distance method used to rerank candidates.}

\item{first_pass_top_k}{Number of first-pass candidates per query. Values
larger than the library size are clamped.}

\item{final_top_k}{Number of reranked hits returned per query.}

\item{query_ids, lib_ids}{IDs aligned to the query and library lists.}

\item{exclude_self}{Whether to remove candidates with identical query/library
IDs. \code{NULL} auto-detects this when the query and library ID vectors are
identical.}

\item{first_pass_block_size}{Reserved for future block-wise first-pass
search. Non-\code{NULL} values currently warn and fall back to full-matrix
first-pass computation.}

\item{return_first_pass}{If \code{TRUE}, return the full query-by-library
first-pass distance matrix and retain first-pass rank and distance columns
in \code{results}. If \code{FALSE}, the matrix is returned as \code{NULL} and those
columns are omitted.}

\item{use_parallel, n_cores}{Parallel settings passed to reranking and the
pair-loop first-pass backend.}

\item{progress}{If \code{TRUE}, emit progress messages.}

\item{query_loss_typ_list, lib_loss_typ_list}{Optional query and library
typical-loss spectra passed through to compatible distance paths.}

\item{query_mref_conf, lib_mref_conf}{Optional query and library Mref
confidence vectors passed through to compatible distance paths.}

\item{...}{Reserved optional arguments. Unsupported names are warned and
ignored in v1.}
}
\value{
A list with long-form \code{results}, per-query \code{candidate_summary}, an
aligned \code{first_pass_distance_matrix} (or \code{NULL} when
\code{return_first_pass = FALSE}), stage-specific parameter lists, and timing
information. Matrix rows and columns follow \code{query_ids} and \code{lib_ids},
respectively. When \code{exclude_self = TRUE}, self-excluded entries are not
counted in \code{candidate_summary$n_finite_first_pass}.
}
\description{
\code{search_topk_rerank()} is a high-level candidate-set reranking API. It first
computes a lightweight query-by-library first-pass distance matrix, keeps the
nearest \code{first_pass_top_k} finite candidates for each query, and reranks only
those candidates with a higher-accuracy method such as \code{"ppm_wasserstein"}.
}
\details{
This is not exact all-vs-all reranking: library entries that do not enter the
first-pass candidate set are not evaluated by \code{rerank_method}. In v1 the
first-pass stage still materializes a full query-by-library matrix via
\code{\link[=compute_distance_matrix_search]{compute_distance_matrix_search()}}. Future LC-HRMS extensions may add
block-wise first-pass search, sparse-bin candidates, or external ANN
candidates.
}
\examples{
params <- eihrms_default_params()
params$distance_method <- "hellinger"
frag <- list(
  a = matrix(c(100, 1), ncol = 2, byrow = TRUE),
  b = matrix(c(101, 1), ncol = 2, byrow = TRUE)
)
search_topk_rerank(
  query_frag_list = frag,
  lib_frag_list = frag,
  params = params,
  first_pass_method = "hellinger",
  rerank_method = "hellinger",
  first_pass_top_k = 2L,
  final_top_k = 1L,
  exclude_self = TRUE,
  progress = FALSE
)

# SI-style reranking simulations can sweep first_pass_top_k over
# c(5, 10, 20, 50, 100), often with final_top_k = 1L.
}
